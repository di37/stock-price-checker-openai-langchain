from utils import *
from openai_functions import *

if __name__ == "__main__":
    # ai_message = model.predict_messages([HumanMessage(content='What is the price of Google stock')], functions=functions)
    # print(open_ai_agent.run("What is the price of Google stock?"))
    # print(open_ai_agent.run("Has google's stock gone up over the past 90 days?"))
    # print(open_ai_agent.run("How much has google's stock gone up over the past 3 months?"))
    # print(open_ai_agent.run("Which stock out of Google, Meta and MSFT has performed best over the past 3 months?"))
    print(open_ai_agent.run("How much has Bitcoin gone up over the past 3 months?"))